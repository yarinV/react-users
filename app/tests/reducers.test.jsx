var expect = require('expect');

const reducers = require('reducers');

describe('Reducers', () => {
  describe('usersReducer', () => {
    it('should fetch users', () => {
      
      expect(1).toBe(1);
    });
  });
});
